<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>List Authors</h1>
    <table border ="1">
        <thead>
            <tr>
                <th>username</th>
                <th>email</th>
                <th>action</th>
            </tr>
        </thead>
        <tbody>
            <tr>{% for author in list %}</tr>
                <tr>
                    <td>{{ author.username }}</td>
                    <td>{{ author.email }}</td>
                    <td>
                        <a href="{{ path('author_edit', {'id': author.id}) }}">Edit</a>
                        <a href="{{ path('author_delete', {'id': author.id}) }}">Delete</a>
                    </td>

                </tr>
            {% endfor %}
        </tbody>
    </table>

</body>
</html>